"""
Type stubs for The Sims 4 game modules.

These stubs provide IDE autocompletion and type checking for code that
targets the game's embedded Python runtime.  They do NOT contain real
implementations -- the game provides those at runtime.

Usage:
    Add the stubs/ directory to your IDE's type stub paths:

    # pyproject.toml
    [tool.mypy]
    mypy_path = "stubs"

    # VS Code settings.json
    "python.analysis.stubPath": "stubs"
"""
